#include "main.h"
/**
 * mul -  a functon that checks for checks for a digit (0 through 9).
 * @a: a - A Variable
 * @b: b - A variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
