#include "main.h"

/**
 * _islower - looks for the lowercase character
 *
 * @c: the character to check
 *
 * Return: returns 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
